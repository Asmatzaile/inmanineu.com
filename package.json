{
  "type": "module",
  "scripts": {
    "dev": "npx eleventy --serve",
    "build": "npx eleventy",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
